export class Ingredient {
    // public name: string;
    // public amount: number;

    // constructor(name: string, amount: number) {
    //     this.name = name;
    //     this.amount = amount;
    // } // jedna z opcji uzycia konstruktora, ale dość długa

    constructor(public name: string, public amount: number) {} // skrócona wersja tego co powyżej
}